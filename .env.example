# Dev (local) — docker compose up -d
LXK_API_KEY=lxk_replace_me
DATABASE_PATH=./data/lexa.db
PORT=3000
COMPOSE_PROJECT_NAME=lexa-dev

# See .env.staging.example and .env.prod.example for remote envs.
# Remote: docker compose -f docker-compose.yml -f docker-compose.staging.yml --env-file .env.staging up -d --build
