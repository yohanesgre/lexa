node_modules/
dist/
.output/
.wrangler/
.dev.vars
.env
.env.staging
.env.prod
data/
bin/
# Private working docs (html viz, specs, superpowers plans) — never published
docs/private/
docs/superpowers/
worker-configuration.d.ts
*.log
.playwright-mcp/
dashboard-test.png
wireframes/dist/

# GitHub App private keys (PEM) — never commit
*.private-key.pem
github-app*.pem

# Command Code local state (taste learning, design reports, permissions) — never commit
.commandcode/
# Agent skills are installed globally (~/.agents/skills/), not vendored
skills/
skills-lock.json
.agents/
# Vendored Effect source for the effect-ts skill (version-pinned by scripts/prepare-effect.sh)
.repos/

# Deepwork session state (git-local, OpenCode-readable)
.slim/deepwork/

# lexa-swarm lane status files (heartbeats, PLAN.md) — ephemeral orchestration state
status/
